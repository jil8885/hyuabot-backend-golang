// Code generated by SQLBoiler 4.15.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// SubwayRoute is an object representing the database table.
type SubwayRoute struct {
	RouteID   int    `boil:"route_id" json:"routeID" toml:"routeID" yaml:"routeID"`
	RouteName string `boil:"route_name" json:"routeName" toml:"routeName" yaml:"routeName"`

	R *subwayRouteR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L subwayRouteL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SubwayRouteColumns = struct {
	RouteID   string
	RouteName string
}{
	RouteID:   "route_id",
	RouteName: "route_name",
}

var SubwayRouteTableColumns = struct {
	RouteID   string
	RouteName string
}{
	RouteID:   "subway_route.route_id",
	RouteName: "subway_route.route_name",
}

// Generated where

var SubwayRouteWhere = struct {
	RouteID   whereHelperint
	RouteName whereHelperstring
}{
	RouteID:   whereHelperint{field: "\"subway_route\".\"route_id\""},
	RouteName: whereHelperstring{field: "\"subway_route\".\"route_name\""},
}

// SubwayRouteRels is where relationship names are stored.
var SubwayRouteRels = struct {
	RouteSubwayRouteStations string
}{
	RouteSubwayRouteStations: "RouteSubwayRouteStations",
}

// subwayRouteR is where relationships are stored.
type subwayRouteR struct {
	RouteSubwayRouteStations SubwayRouteStationSlice `boil:"RouteSubwayRouteStations" json:"RouteSubwayRouteStations" toml:"RouteSubwayRouteStations" yaml:"RouteSubwayRouteStations"`
}

// NewStruct creates a new relationship struct
func (*subwayRouteR) NewStruct() *subwayRouteR {
	return &subwayRouteR{}
}

func (r *subwayRouteR) GetRouteSubwayRouteStations() SubwayRouteStationSlice {
	if r == nil {
		return nil
	}
	return r.RouteSubwayRouteStations
}

// subwayRouteL is where Load methods for each relationship are stored.
type subwayRouteL struct{}

var (
	subwayRouteAllColumns            = []string{"route_id", "route_name"}
	subwayRouteColumnsWithoutDefault = []string{"route_id", "route_name"}
	subwayRouteColumnsWithDefault    = []string{}
	subwayRoutePrimaryKeyColumns     = []string{"route_id"}
	subwayRouteGeneratedColumns      = []string{}
)

type (
	// SubwayRouteSlice is an alias for a slice of pointers to SubwayRoute.
	// This should almost always be used instead of []SubwayRoute.
	SubwayRouteSlice []*SubwayRoute

	subwayRouteQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	subwayRouteType                 = reflect.TypeOf(&SubwayRoute{})
	subwayRouteMapping              = queries.MakeStructMapping(subwayRouteType)
	subwayRoutePrimaryKeyMapping, _ = queries.BindMapping(subwayRouteType, subwayRouteMapping, subwayRoutePrimaryKeyColumns)
	subwayRouteInsertCacheMut       sync.RWMutex
	subwayRouteInsertCache          = make(map[string]insertCache)
	subwayRouteUpdateCacheMut       sync.RWMutex
	subwayRouteUpdateCache          = make(map[string]updateCache)
	subwayRouteUpsertCacheMut       sync.RWMutex
	subwayRouteUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single subwayRoute record from the query.
func (q subwayRouteQuery) One(ctx context.Context, exec boil.ContextExecutor) (*SubwayRoute, error) {
	o := &SubwayRoute{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for subway_route")
	}

	return o, nil
}

// All returns all SubwayRoute records from the query.
func (q subwayRouteQuery) All(ctx context.Context, exec boil.ContextExecutor) (SubwayRouteSlice, error) {
	var o []*SubwayRoute

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to SubwayRoute slice")
	}

	return o, nil
}

// Count returns the count of all SubwayRoute records in the query.
func (q subwayRouteQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count subway_route rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q subwayRouteQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if subway_route exists")
	}

	return count > 0, nil
}

// RouteSubwayRouteStations retrieves all the subway_route_station's SubwayRouteStations with an executor via route_id column.
func (o *SubwayRoute) RouteSubwayRouteStations(mods ...qm.QueryMod) subwayRouteStationQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"subway_route_station\".\"route_id\"=?", o.RouteID),
	)

	return SubwayRouteStations(queryMods...)
}

// LoadRouteSubwayRouteStations allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (subwayRouteL) LoadRouteSubwayRouteStations(ctx context.Context, e boil.ContextExecutor, singular bool, maybeSubwayRoute interface{}, mods queries.Applicator) error {
	var slice []*SubwayRoute
	var object *SubwayRoute

	if singular {
		var ok bool
		object, ok = maybeSubwayRoute.(*SubwayRoute)
		if !ok {
			object = new(SubwayRoute)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeSubwayRoute)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeSubwayRoute))
			}
		}
	} else {
		s, ok := maybeSubwayRoute.(*[]*SubwayRoute)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeSubwayRoute)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeSubwayRoute))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &subwayRouteR{}
		}
		args = append(args, object.RouteID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &subwayRouteR{}
			}

			for _, a := range args {
				if a == obj.RouteID {
					continue Outer
				}
			}

			args = append(args, obj.RouteID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`subway_route_station`),
		qm.WhereIn(`subway_route_station.route_id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load subway_route_station")
	}

	var resultSlice []*SubwayRouteStation
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice subway_route_station")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on subway_route_station")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for subway_route_station")
	}

	if singular {
		object.R.RouteSubwayRouteStations = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &subwayRouteStationR{}
			}
			foreign.R.Route = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.RouteID == foreign.RouteID {
				local.R.RouteSubwayRouteStations = append(local.R.RouteSubwayRouteStations, foreign)
				if foreign.R == nil {
					foreign.R = &subwayRouteStationR{}
				}
				foreign.R.Route = local
				break
			}
		}
	}

	return nil
}

// AddRouteSubwayRouteStations adds the given related objects to the existing relationships
// of the subway_route, optionally inserting them as new records.
// Appends related to o.R.RouteSubwayRouteStations.
// Sets related.R.Route appropriately.
func (o *SubwayRoute) AddRouteSubwayRouteStations(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*SubwayRouteStation) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.RouteID = o.RouteID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"subway_route_station\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"route_id"}),
				strmangle.WhereClause("\"", "\"", 2, subwayRouteStationPrimaryKeyColumns),
			)
			values := []interface{}{o.RouteID, rel.StationID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.RouteID = o.RouteID
		}
	}

	if o.R == nil {
		o.R = &subwayRouteR{
			RouteSubwayRouteStations: related,
		}
	} else {
		o.R.RouteSubwayRouteStations = append(o.R.RouteSubwayRouteStations, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &subwayRouteStationR{
				Route: o,
			}
		} else {
			rel.R.Route = o
		}
	}
	return nil
}

// SubwayRoutes retrieves all the records using an executor.
func SubwayRoutes(mods ...qm.QueryMod) subwayRouteQuery {
	mods = append(mods, qm.From("\"subway_route\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"subway_route\".*"})
	}

	return subwayRouteQuery{q}
}

// FindSubwayRoute retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSubwayRoute(ctx context.Context, exec boil.ContextExecutor, routeID int, selectCols ...string) (*SubwayRoute, error) {
	subwayRouteObj := &SubwayRoute{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"subway_route\" where \"route_id\"=$1", sel,
	)

	q := queries.Raw(query, routeID)

	err := q.Bind(ctx, exec, subwayRouteObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from subway_route")
	}

	return subwayRouteObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *SubwayRoute) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no subway_route provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(subwayRouteColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	subwayRouteInsertCacheMut.RLock()
	cache, cached := subwayRouteInsertCache[key]
	subwayRouteInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			subwayRouteAllColumns,
			subwayRouteColumnsWithDefault,
			subwayRouteColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(subwayRouteType, subwayRouteMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(subwayRouteType, subwayRouteMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"subway_route\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"subway_route\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into subway_route")
	}

	if !cached {
		subwayRouteInsertCacheMut.Lock()
		subwayRouteInsertCache[key] = cache
		subwayRouteInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the SubwayRoute.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *SubwayRoute) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	subwayRouteUpdateCacheMut.RLock()
	cache, cached := subwayRouteUpdateCache[key]
	subwayRouteUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			subwayRouteAllColumns,
			subwayRoutePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update subway_route, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"subway_route\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, subwayRoutePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(subwayRouteType, subwayRouteMapping, append(wl, subwayRoutePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update subway_route row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for subway_route")
	}

	if !cached {
		subwayRouteUpdateCacheMut.Lock()
		subwayRouteUpdateCache[key] = cache
		subwayRouteUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q subwayRouteQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for subway_route")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for subway_route")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SubwayRouteSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), subwayRoutePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"subway_route\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, subwayRoutePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in subwayRoute slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all subwayRoute")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *SubwayRoute) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no subway_route provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(subwayRouteColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	subwayRouteUpsertCacheMut.RLock()
	cache, cached := subwayRouteUpsertCache[key]
	subwayRouteUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			subwayRouteAllColumns,
			subwayRouteColumnsWithDefault,
			subwayRouteColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			subwayRouteAllColumns,
			subwayRoutePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert subway_route, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(subwayRoutePrimaryKeyColumns))
			copy(conflict, subwayRoutePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"subway_route\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(subwayRouteType, subwayRouteMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(subwayRouteType, subwayRouteMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert subway_route")
	}

	if !cached {
		subwayRouteUpsertCacheMut.Lock()
		subwayRouteUpsertCache[key] = cache
		subwayRouteUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single SubwayRoute record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SubwayRoute) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no SubwayRoute provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), subwayRoutePrimaryKeyMapping)
	sql := "DELETE FROM \"subway_route\" WHERE \"route_id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from subway_route")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for subway_route")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q subwayRouteQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no subwayRouteQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from subway_route")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for subway_route")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SubwayRouteSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), subwayRoutePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"subway_route\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, subwayRoutePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from subwayRoute slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for subway_route")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SubwayRoute) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindSubwayRoute(ctx, exec, o.RouteID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SubwayRouteSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := SubwayRouteSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), subwayRoutePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"subway_route\".* FROM \"subway_route\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, subwayRoutePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in SubwayRouteSlice")
	}

	*o = slice

	return nil
}

// SubwayRouteExists checks if the SubwayRoute row exists.
func SubwayRouteExists(ctx context.Context, exec boil.ContextExecutor, routeID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"subway_route\" where \"route_id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, routeID)
	}
	row := exec.QueryRowContext(ctx, sql, routeID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if subway_route exists")
	}

	return exists, nil
}

// Exists checks if the SubwayRoute row exists.
func (o *SubwayRoute) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return SubwayRouteExists(ctx, exec, o.RouteID)
}
