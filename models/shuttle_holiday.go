// Code generated by SQLBoiler 4.15.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// ShuttleHoliday is an object representing the database table.
type ShuttleHoliday struct {
	HolidayDate  time.Time `boil:"holiday_date" json:"holidayDate" toml:"holidayDate" yaml:"holidayDate"`
	HolidayType  string    `boil:"holiday_type" json:"holidayType" toml:"holidayType" yaml:"holidayType"`
	CalendarType string    `boil:"calendar_type" json:"calendarType" toml:"calendarType" yaml:"calendarType"`

	R *shuttleHolidayR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L shuttleHolidayL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ShuttleHolidayColumns = struct {
	HolidayDate  string
	HolidayType  string
	CalendarType string
}{
	HolidayDate:  "holiday_date",
	HolidayType:  "holiday_type",
	CalendarType: "calendar_type",
}

var ShuttleHolidayTableColumns = struct {
	HolidayDate  string
	HolidayType  string
	CalendarType string
}{
	HolidayDate:  "shuttle_holiday.holiday_date",
	HolidayType:  "shuttle_holiday.holiday_type",
	CalendarType: "shuttle_holiday.calendar_type",
}

// Generated where

var ShuttleHolidayWhere = struct {
	HolidayDate  whereHelpertime_Time
	HolidayType  whereHelperstring
	CalendarType whereHelperstring
}{
	HolidayDate:  whereHelpertime_Time{field: "\"shuttle_holiday\".\"holiday_date\""},
	HolidayType:  whereHelperstring{field: "\"shuttle_holiday\".\"holiday_type\""},
	CalendarType: whereHelperstring{field: "\"shuttle_holiday\".\"calendar_type\""},
}

// ShuttleHolidayRels is where relationship names are stored.
var ShuttleHolidayRels = struct {
}{}

// shuttleHolidayR is where relationships are stored.
type shuttleHolidayR struct {
}

// NewStruct creates a new relationship struct
func (*shuttleHolidayR) NewStruct() *shuttleHolidayR {
	return &shuttleHolidayR{}
}

// shuttleHolidayL is where Load methods for each relationship are stored.
type shuttleHolidayL struct{}

var (
	shuttleHolidayAllColumns            = []string{"holiday_date", "holiday_type", "calendar_type"}
	shuttleHolidayColumnsWithoutDefault = []string{"holiday_date", "holiday_type", "calendar_type"}
	shuttleHolidayColumnsWithDefault    = []string{}
	shuttleHolidayPrimaryKeyColumns     = []string{"holiday_date", "holiday_type", "calendar_type"}
	shuttleHolidayGeneratedColumns      = []string{}
)

type (
	// ShuttleHolidaySlice is an alias for a slice of pointers to ShuttleHoliday.
	// This should almost always be used instead of []ShuttleHoliday.
	ShuttleHolidaySlice []*ShuttleHoliday

	shuttleHolidayQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	shuttleHolidayType                 = reflect.TypeOf(&ShuttleHoliday{})
	shuttleHolidayMapping              = queries.MakeStructMapping(shuttleHolidayType)
	shuttleHolidayPrimaryKeyMapping, _ = queries.BindMapping(shuttleHolidayType, shuttleHolidayMapping, shuttleHolidayPrimaryKeyColumns)
	shuttleHolidayInsertCacheMut       sync.RWMutex
	shuttleHolidayInsertCache          = make(map[string]insertCache)
	shuttleHolidayUpdateCacheMut       sync.RWMutex
	shuttleHolidayUpdateCache          = make(map[string]updateCache)
	shuttleHolidayUpsertCacheMut       sync.RWMutex
	shuttleHolidayUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single shuttleHoliday record from the query.
func (q shuttleHolidayQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ShuttleHoliday, error) {
	o := &ShuttleHoliday{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for shuttle_holiday")
	}

	return o, nil
}

// All returns all ShuttleHoliday records from the query.
func (q shuttleHolidayQuery) All(ctx context.Context, exec boil.ContextExecutor) (ShuttleHolidaySlice, error) {
	var o []*ShuttleHoliday

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to ShuttleHoliday slice")
	}

	return o, nil
}

// Count returns the count of all ShuttleHoliday records in the query.
func (q shuttleHolidayQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count shuttle_holiday rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q shuttleHolidayQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if shuttle_holiday exists")
	}

	return count > 0, nil
}

// ShuttleHolidays retrieves all the records using an executor.
func ShuttleHolidays(mods ...qm.QueryMod) shuttleHolidayQuery {
	mods = append(mods, qm.From("\"shuttle_holiday\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"shuttle_holiday\".*"})
	}

	return shuttleHolidayQuery{q}
}

// FindShuttleHoliday retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindShuttleHoliday(ctx context.Context, exec boil.ContextExecutor, holidayDate time.Time, holidayType string, calendarType string, selectCols ...string) (*ShuttleHoliday, error) {
	shuttleHolidayObj := &ShuttleHoliday{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"shuttle_holiday\" where \"holiday_date\"=$1 AND \"holiday_type\"=$2 AND \"calendar_type\"=$3", sel,
	)

	q := queries.Raw(query, holidayDate, holidayType, calendarType)

	err := q.Bind(ctx, exec, shuttleHolidayObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from shuttle_holiday")
	}

	return shuttleHolidayObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ShuttleHoliday) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no shuttle_holiday provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(shuttleHolidayColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	shuttleHolidayInsertCacheMut.RLock()
	cache, cached := shuttleHolidayInsertCache[key]
	shuttleHolidayInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			shuttleHolidayAllColumns,
			shuttleHolidayColumnsWithDefault,
			shuttleHolidayColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(shuttleHolidayType, shuttleHolidayMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(shuttleHolidayType, shuttleHolidayMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"shuttle_holiday\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"shuttle_holiday\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into shuttle_holiday")
	}

	if !cached {
		shuttleHolidayInsertCacheMut.Lock()
		shuttleHolidayInsertCache[key] = cache
		shuttleHolidayInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the ShuttleHoliday.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ShuttleHoliday) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	shuttleHolidayUpdateCacheMut.RLock()
	cache, cached := shuttleHolidayUpdateCache[key]
	shuttleHolidayUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			shuttleHolidayAllColumns,
			shuttleHolidayPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update shuttle_holiday, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"shuttle_holiday\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, shuttleHolidayPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(shuttleHolidayType, shuttleHolidayMapping, append(wl, shuttleHolidayPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update shuttle_holiday row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for shuttle_holiday")
	}

	if !cached {
		shuttleHolidayUpdateCacheMut.Lock()
		shuttleHolidayUpdateCache[key] = cache
		shuttleHolidayUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q shuttleHolidayQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for shuttle_holiday")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for shuttle_holiday")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ShuttleHolidaySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), shuttleHolidayPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"shuttle_holiday\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, shuttleHolidayPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in shuttleHoliday slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all shuttleHoliday")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ShuttleHoliday) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no shuttle_holiday provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(shuttleHolidayColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	shuttleHolidayUpsertCacheMut.RLock()
	cache, cached := shuttleHolidayUpsertCache[key]
	shuttleHolidayUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			shuttleHolidayAllColumns,
			shuttleHolidayColumnsWithDefault,
			shuttleHolidayColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			shuttleHolidayAllColumns,
			shuttleHolidayPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert shuttle_holiday, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(shuttleHolidayPrimaryKeyColumns))
			copy(conflict, shuttleHolidayPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"shuttle_holiday\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(shuttleHolidayType, shuttleHolidayMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(shuttleHolidayType, shuttleHolidayMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert shuttle_holiday")
	}

	if !cached {
		shuttleHolidayUpsertCacheMut.Lock()
		shuttleHolidayUpsertCache[key] = cache
		shuttleHolidayUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single ShuttleHoliday record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ShuttleHoliday) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no ShuttleHoliday provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), shuttleHolidayPrimaryKeyMapping)
	sql := "DELETE FROM \"shuttle_holiday\" WHERE \"holiday_date\"=$1 AND \"holiday_type\"=$2 AND \"calendar_type\"=$3"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from shuttle_holiday")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for shuttle_holiday")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q shuttleHolidayQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no shuttleHolidayQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from shuttle_holiday")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for shuttle_holiday")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ShuttleHolidaySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), shuttleHolidayPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"shuttle_holiday\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, shuttleHolidayPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from shuttleHoliday slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for shuttle_holiday")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ShuttleHoliday) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindShuttleHoliday(ctx, exec, o.HolidayDate, o.HolidayType, o.CalendarType)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ShuttleHolidaySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ShuttleHolidaySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), shuttleHolidayPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"shuttle_holiday\".* FROM \"shuttle_holiday\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, shuttleHolidayPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ShuttleHolidaySlice")
	}

	*o = slice

	return nil
}

// ShuttleHolidayExists checks if the ShuttleHoliday row exists.
func ShuttleHolidayExists(ctx context.Context, exec boil.ContextExecutor, holidayDate time.Time, holidayType string, calendarType string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"shuttle_holiday\" where \"holiday_date\"=$1 AND \"holiday_type\"=$2 AND \"calendar_type\"=$3 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, holidayDate, holidayType, calendarType)
	}
	row := exec.QueryRowContext(ctx, sql, holidayDate, holidayType, calendarType)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if shuttle_holiday exists")
	}

	return exists, nil
}

// Exists checks if the ShuttleHoliday row exists.
func (o *ShuttleHoliday) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ShuttleHolidayExists(ctx, exec, o.HolidayDate, o.HolidayType, o.CalendarType)
}
